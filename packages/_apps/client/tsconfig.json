{
    "extends": "../../../tsconfig-base.json",
    "include": [
      "@types/global.d.ts", "src" // *** The files TypeScript should type check ***
  ],
    "exclude": ["node_modules", "dist"] // *** The files to not type check ***
}